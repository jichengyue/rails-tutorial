<% provide(:title, 'All users') %>
<h1>All users</h1>

<ul class="users">
 <%= render @users %>
  <!--
  <% @users.each do |user| %>
    <%= render user %>
  <% end %> 
  -->
</ul>
<%= will_paginate %>